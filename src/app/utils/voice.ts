type RecognitionEvent = { results: { 0: { 0: { transcript: string } } } };
interface Recognition { lang: string; start(): void; onresult: (event: RecognitionEvent) => void; onerror: () => void; }
type RecognitionConstructor = new () => Recognition;

export function voiceOutputSupported(): boolean { return typeof window !== 'undefined' && 'speechSynthesis' in window; }
export function voiceInputSupported(): boolean { if(typeof window==='undefined')return false;const speechWindow=window as unknown as {SpeechRecognition?:RecognitionConstructor;webkitSpeechRecognition?:RecognitionConstructor};return Boolean(speechWindow.SpeechRecognition??speechWindow.webkitSpeechRecognition); }
export function speak(text:string,lang='en-IN'):boolean{if(!voiceOutputSupported())return false;window.speechSynthesis.cancel();const utterance=new SpeechSynthesisUtterance(text);utterance.lang=lang;const preferred=window.speechSynthesis.getVoices().find(voice=>voice.lang.toLowerCase().startsWith(lang.slice(0,2).toLowerCase()));if(preferred)utterance.voice=preferred;window.speechSynthesis.speak(utterance);return true;}
export function listen(lang='en-IN'):Promise<string>{return new Promise((resolve,reject)=>{const speechWindow=window as unknown as {SpeechRecognition?:RecognitionConstructor;webkitSpeechRecognition?:RecognitionConstructor};const Recognition=speechWindow.SpeechRecognition??speechWindow.webkitSpeechRecognition;if(!Recognition){reject(new Error('Voice input is not supported on this browser'));return}const recognition=new Recognition();recognition.lang=lang;recognition.onresult=event=>resolve(event.results[0][0].transcript);recognition.onerror=()=>reject(new Error('Voice input was unavailable'));recognition.start();});}
