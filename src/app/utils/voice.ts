export function speak(text: string, lang = 'en-IN'): void { if (!('speechSynthesis' in window)) return; window.speechSynthesis.cancel(); const utterance = new SpeechSynthesisUtterance(text); utterance.lang = lang; window.speechSynthesis.speak(utterance); }
export function listen(lang = 'en-IN'): Promise<string> { return new Promise((resolve,reject)=>{ const Recognition=(window as unknown as {SpeechRecognition?:new()=>{lang:string;start():void;onresult:(e:{results:Array<Array<{transcript:string}>>})=>void;onerror:()=>void}}).SpeechRecognition; if(!Recognition){reject(new Error('Voice input is not supported on this browser'));return} const recognition=new Recognition();recognition.lang=lang;recognition.onresult=e=>resolve(e.results[0][0].transcript);recognition.onerror=()=>reject(new Error('Voice input was unavailable'));recognition.start(); }); }
