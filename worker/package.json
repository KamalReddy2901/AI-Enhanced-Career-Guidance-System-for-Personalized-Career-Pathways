{
  "name": "careercaseai-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets:set": "wrangler secret put GROQ_API_KEYS && wrangler secret put SUPABASE_URL && wrangler secret put SUPABASE_SERVICE_KEY"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.101.0"
  }
}
